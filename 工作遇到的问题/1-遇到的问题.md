

**原生axios发送post请求**

```
let url="xxx/xxx";
axios.post(
    url,
    params,
    {headers: {'Content-Type': 'application/json'}}//传递的格式
).then(res=>{
    if(res.code=='200'){
    console.log(res.data);
    }
});
```